.section .data

meno:
	.ascii "-"

.section .text

.global numero_negativo

.type numero_negativo, @function

numero_negativo:
	cmp $0, %eax
	jge fine
	
	neg %eax
	pushl %eax

	movl $4, %eax
	movl $1, %ebx
	leal meno, %ecx
	movl $1, %edx
	int $0x80
		
	popl %eax

fine:
	ret
